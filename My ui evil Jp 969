-- ========================================================
-- EVIL JP 969 CUSTOM GUI
-- GitHub Ready Script
-- ========================================================

local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ContentProvider = game:GetService("ContentProvider")

-- YOUR ROBLOX ASSET IDs
local INTRO_IMAGE_ID = "rbxassetid://72174703226327" 
local LOGO_IMAGE_ID  = "rbxassetid://115024591759701" 

local TargetParent = (gethui and gethui()) or CoreGui

-- ========================================================
-- 1. INTRO LOADING SCREEN
-- ========================================================
local IntroGui = Instance.new("ScreenGui")
IntroGui.Name = "EvilJp969_Intro"
IntroGui.IgnoreGuiInset = true
IntroGui.ResetOnSpawn = false
IntroGui.Parent = TargetParent

local IntroBg = Instance.new("Frame")
IntroBg.Size = UDim2.new(1, 0, 1, 0)
IntroBg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
IntroBg.Parent = IntroGui

local IntroImage = Instance.new("ImageLabel")
IntroImage.Size = UDim2.new(0.6, 0, 0.6, 0)
IntroImage.Position = UDim2.new(0.5, 0, 0.5, 0)
IntroImage.AnchorPoint = Vector2.new(0.5, 0.5)
IntroImage.BackgroundTransparency = 1
IntroImage.ImageTransparency = 1
IntroImage.Image = INTRO_IMAGE_ID
IntroImage.Parent = IntroBg

local AspectRatio = Instance.new("UIAspectRatioConstraint")
AspectRatio.Parent = IntroImage

-- Preload Intro Graphic
pcall(function() ContentProvider:PreloadAsync({IntroImage}) end)

-- Play Intro Animation (Fade In -> Hold -> Fade Out)
local fadeInfo = TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
TweenService:Create(IntroImage, fadeInfo, {ImageTransparency = 0}):Play()
task.wait(2.5)

TweenService:Create(IntroImage, fadeInfo, {ImageTransparency = 1}):Play()
local fadeBg = TweenService:Create(IntroBg, fadeInfo, {BackgroundTransparency = 1})
fadeBg:Play()
fadeBg.Completed:Wait()

IntroGui:Destroy()

-- ========================================================
-- 2. MAIN HUB UI (EVIL JP 969)
-- ========================================================
local MainGui = Instance.new("ScreenGui")
MainGui.Name = "EvilJp969_Hub"
MainGui.ResetOnSpawn = false
MainGui.Parent = TargetParent

-- Main Window Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 520, 0, 320)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 18, 26)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = MainGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(130, 60, 220)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- Top Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(28, 25, 38)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleBar

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(1, -20, 1, 0)
TitleText.Position = UDim2.new(0, 15, 0, 0)
TitleText.Text = "evil Jp 969"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.Font = Enum.Font.GothamBold
TitleText.TextSize = 15
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.BackgroundTransparency = 1
TitleText.Parent = TitleBar

-- Sidebar Layout
local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 120, 1, -40)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 14, 20)
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame

local SideLayout = Instance.new("UIListLayout")
SideLayout.Padding = UDim.new(0, 6)
SideLayout.SortOrder = Enum.SortOrder.LayoutOrder
SideLayout.Parent = Sidebar

local SidePadding = Instance.new("UIPadding")
SidePadding.PaddingTop = UDim.new(0, 10)
SidePadding.PaddingLeft = UDim.new(0, 8)
SidePadding.PaddingRight = UDim.new(0, 8)
SidePadding.Parent = Sidebar

-- Content Container
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -135, 1, -50)
ContentFrame.Position = UDim2.new(0, 130, 0, 45)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

local ContentLayout = Instance.new("UIListLayout")
ContentLayout.Padding = UDim.new(0, 8)
ContentLayout.Parent = ContentFrame

-- Helper UI Functions
local function CreateButton(text, callback)
	local Btn = Instance.new("TextButton")
	Btn.Size = UDim2.new(1, -10, 0, 35)
	Btn.BackgroundColor3 = Color3.fromRGB(32, 28, 44)
	Btn.Text = text
	Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	Btn.Font = Enum.Font.GothamSemibold
	Btn.TextSize = 13
	Btn.Parent = ContentFrame
	
	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = Btn
	
	Btn.MouseButton1Click:Connect(function()
		TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(130, 60, 220)}):Play()
		task.wait(0.1)
		TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(32, 28, 44)}):Play()
		callback()
	end)
end

-- Example Controls inside UI
CreateButton("Click Me", function() print("evil Jp 969 action triggered!") end)

-- ========================================================
-- 3. MOVABLE & CLICKABLE FLOATING LOGO ICON (TOGGLE)
-- ========================================================
local ToggleBtn = Instance.new("ImageButton")
ToggleBtn.Name = "EvilJp969_LogoIcon"
ToggleBtn.Size = UDim2.new(0, 55, 0, 55)
ToggleBtn.Position = UDim2.new(0, 20, 0.5, -27)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleBtn.Image = LOGO_IMAGE_ID
ToggleBtn.Parent = MainGui

local IconCorner = Instance.new("UICorner")
IconCorner.CornerRadius = UDim.new(1, 0) -- Makes icon circular
IconCorner.Parent = ToggleBtn

local IconStroke = Instance.new("UIStroke")
IconStroke.Color = Color3.fromRGB(220, 30, 30)
IconStroke.Thickness = 2
IconStroke.Parent = ToggleBtn

-- Preload Logo
pcall(function() ContentProvider:PreloadAsync({ToggleBtn}) end)

-- Click & Drag Mechanics for Icon
local draggingIcon = false
local dragStart, startPos
local wasDragged = false

ToggleBtn.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		draggingIcon = true
		wasDragged = false
		dragStart = input.Position
		startPos = ToggleBtn.Position
		
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				draggingIcon = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if draggingIcon and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		if math.abs(delta.X) > 5 or math.abs(delta.Y) > 5 then
			wasDragged = true
		end
		ToggleBtn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

ToggleBtn.MouseButton1Click:Connect(function()
	if not wasDragged then
		MainFrame.Visible = not MainFrame.Visible
	end
end)

-- Main Frame Dragging Mechanics
local draggingFrame, frameDragStart, frameStartPos
TitleBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		draggingFrame = true
		frameDragStart = input.Position
		frameStartPos = MainFrame.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if draggingFrame and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - frameDragStart
		MainFrame.Position = UDim2.new(frameStartPos.X.Scale, frameStartPos.X.Offset + delta.X, frameStartPos.Y.Scale, frameStartPos.Y.Offset + delta.Y)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		draggingFrame = false
	end
end)
